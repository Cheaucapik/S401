datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Formation {
  id          Int   @id @default(autoincrement())
  title       String
  color       String
  colorTitle  String
  status      String
  duration    String
  presentiel  Boolean
}